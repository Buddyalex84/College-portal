# Core Django stack actually used by the project
Django>=5.1,<5.2
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.4,<5.0

# Image handling for media uploads
Pillow>=10.4

# Loads variables from .env (used by chatbot/views.py)
python-dotenv>=1.0

# Optional: install separately if you want the AI chatbot to work.
# This package is not on PyPI and must be obtained from the Emergent platform.
# emergentintegrations
